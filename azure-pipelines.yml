# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main
- develop

pool:
  vmImage: ubuntu-latest

steps:
- script: 'Running Azure Pipeline'

- task: Bash@3
  echo 'Starting my first bash task'
#  whoami
  
  echo 'Running Flyway'
  cd C:\Users\ajinth.christudas\Documents\flyway\flyway-8.5.9\
  echo 'In the flyway directory'